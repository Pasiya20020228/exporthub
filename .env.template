# ============================================================================
# Global environment template for the ExportHub platform.
# Copy this file to `.env` and fill in the values for the deployment target.
# ============================================================================

# ---------------------------- Backend Service ------------------------------
# BACKEND_PORT: Port number that the backend HTTP server binds to.
BACKEND_PORT=8000
# BACKEND_DEBUG: Enables verbose logging and debug features (true/false).
BACKEND_DEBUG=false
# DATABASE_URL: Connection string for the primary application database (required).
DATABASE_URL=
# BACKEND_SECRET_KEY: Secret used for signing sessions or JWT tokens (required).
BACKEND_SECRET_KEY=
# BACKEND_STORAGE_BUCKET: Name of the object storage bucket for uploads (required).
BACKEND_STORAGE_BUCKET=

# ---------------------------- Frontend Service -----------------------------
# FRONTEND_PORT: Port for the frontend development server (e.g. Vite/Next).
FRONTEND_PORT=3000
# FRONTEND_API_BASE_URL: Base URL for API requests from the frontend (required).
FRONTEND_API_BASE_URL=http://localhost:8000/api
# FRONTEND_ENABLE_ANALYTICS: Enable analytics scripts in production builds (true/false).
FRONTEND_ENABLE_ANALYTICS=false
# FRONTEND_SENTRY_DSN: Optional DSN for Sentry error reporting.
FRONTEND_SENTRY_DSN=

# ------------------------- Infrastructure / Secrets ------------------------
# AWS_REGION: AWS region used for infrastructure resources.
AWS_REGION=us-east-1
# AWS_SSM_PARAMETER_PATH: Base path in AWS SSM Parameter Store for secrets.
AWS_SSM_PARAMETER_PATH=/exporthub/
# VAULT_ADDR: URL of the HashiCorp Vault server (if using Vault).
VAULT_ADDR=
# VAULT_NAMESPACE: Namespace within Vault for this environment (optional).
VAULT_NAMESPACE=
# GITHUB_ACTIONS_SECRET_PREFIX: Prefix used when referencing GitHub Actions secrets.
GITHUB_ACTIONS_SECRET_PREFIX=EXPORTHUB_
